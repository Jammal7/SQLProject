-- Compute the total number of unique indicator codes
SELECT COUNT(DISTINCT Indicator_Code) FROM NewTable

-- A query to retrieve the unique indicator codes
SELECT DISTINCT Indicator_Code FROM NewTable